# Build context is the repo root (see infra/docker-compose.yml -> api.build.context: ..)
# Keep the context small: only /shared and /server are needed to build the API image.
**/node_modules
**/dist
**/*.tsbuildinfo
app
infra
.git
.github
**/.env
**/.env.*
!**/.env.example
*.md
